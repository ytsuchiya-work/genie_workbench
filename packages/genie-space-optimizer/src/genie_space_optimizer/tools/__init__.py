"""Operator-facing CLI tools for genie-space-optimizer.

Each tool is invoked via `python -m genie_space_optimizer.tools.<name>`.
Read-only diagnostics live alongside write-side commands; each module's
docstring states which it is.
"""
